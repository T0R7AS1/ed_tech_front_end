import {
    Box,
    useTheme,
    TextField,
    InputAdornment,
    IconButton,
    Icon,
    Button,
} from '@mui/material';
import AuthIndex from './index';

export default function Products(){
    return (
        <AuthIndex>
            <Box>
                test
            </Box>
        </AuthIndex>
    );
}